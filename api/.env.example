# GitHub Personal Access Token (required)
# How to generate:
# 1. Go to GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
#    URL: https://github.com/settings/tokens
# 2. Click "Generate new token" > "Generate new token (classic)"
# 3. Give it a descriptive name (e.g., "Leo Git Statistics API")
# 4. Select the following scopes/permissions:
#    - repo (Read access to your repositories to fetch commit history and statistics)
#      Note: Despite the name, this is only used for READ-ONLY access to analyze your code
#    - read:user (Read your public profile information)
# 5. Click "Generate token" and copy the token immediately (you won't see it again)
# 6. Paste the token below
# Note: This is a PERSONAL access token from your GitHub profile, not a repository token
GITHUB_TOKEN=your_github_token_here

# API Configuration (optional)
PORT=5000
FLASK_DEBUG=false
API_HOST=localhost:5000
