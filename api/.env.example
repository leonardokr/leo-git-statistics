# GitHub Personal Access Token (required)
# For public API deployments, use a token with ONLY "public_repo" scope
# to prevent leaking private repository data to callers.
# How to generate:
# 1. Go to GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
#    URL: https://github.com/settings/tokens
# 2. Click "Generate new token" > "Generate new token (classic)"
# 3. Give it a descriptive name (e.g., "Leo Git Statistics API")
# 4. Select scopes:
#    - public_repo (recommended for public deployments)
#    - repo (only if ALLOW_PRIVATE_REPOS=true and you understand the risks)
#    - read:user (read your public profile information)
# 5. Click "Generate token" and copy the token immediately
GITHUB_TOKEN=your_github_token_here

# API Configuration
PORT=8000
WORKERS=4

# Security: Private repository access (default: false)
# When false, private repos are always excluded from responses for
# requests that use the server token. Users who provide their own
# token via X-GitHub-Token can still see their own private repos.
ALLOW_PRIVATE_REPOS=false

# Security: API authentication (default: disabled)
# Set to "true" to require a valid API key on all /users/* endpoints.
# /health and /docs remain public.
API_AUTH_ENABLED=false

# Comma-separated list of valid API keys (required when API_AUTH_ENABLED=true)
API_KEYS=

# Security: CORS origins (default: * for development)
# Comma-separated list of allowed origins for production deployments.
CORS_ORIGINS=*

# Rate limiting (uses slowapi format, e.g. "30/minute", "100/hour")
RATE_LIMIT_DEFAULT=30/minute
RATE_LIMIT_AUTH=100/minute
RATE_LIMIT_HEAVY=10/minute

# Database: path to the SQLite traffic database
# DATABASE_PATH=src/db/traffic.db

# Database: path to the snapshots SQLite database
# SNAPSHOTS_DB_PATH=src/db/snapshots.db

# Database: path to the webhooks SQLite database
# WEBHOOKS_DB_PATH=src/db/webhooks.db
